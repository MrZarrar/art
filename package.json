{
  "name": "mushaf-zarrar-art-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-content": "node scripts/sync-content.mjs",
    "dev": "npm run sync-content && vite",
    "test": "node --test scripts/*.test.mjs src/*.test.js",
    "build": "npm run sync-content && vite build",
    "check": "npm test && npm run build",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "sharp": "0.34.3",
    "vite": "6.4.2"
  },
  "allowScripts": {
    "sharp@0.34.3": true,
    "esbuild@0.25.12": true
  }
}
